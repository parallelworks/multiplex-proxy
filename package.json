{
  "name": "pw-multiplex-proxy",
  "version": "1.0.0",
  "description": "Parallel Works ACTIVATE companion: multiplex multiple HTTPS sites from a workspace through one local port via SNI routing over pw ssh tunnels",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node tests/sni-proxy.test.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
